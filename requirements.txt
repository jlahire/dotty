fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
ollama>=0.4.0
pillow

# Web security (optional — degrades to basic header checks)
drheader

# Interactive browser session (optional — needs: playwright install chromium)
playwright

# Forensic (optional — features degrade gracefully)
pytsk3
dissect.target
volatility3
pycdlib
lz4
psutil
